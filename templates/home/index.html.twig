{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
  
  <section>
    <p id="fond" class="accroche">A vous de dévaler les pentes !</p>
  </section>

  <div class="container">
    <section class="haut flex espace liste" id="tricks">
      {% for trick in tricks %}
      {% for media in trick.media %}
        {% if media.isMain == 1 %}
          <article>
            <img src={{ media.path }} alt={{ trick.title }} class="tricks" />
            <div class="details flex">
              <p><a href={{'tricks/' ~ trick.id }}>{{ trick.title }}</a></p>
              <div class="bouton">
                <a href=""><img src="img/pencil.png" alt="Editer" /></a>
                <a href=""><img src="img/corbeille.png" alt="Corbeille" /></a>
              </div>
            </div>
          </article>
        {% endif %}
      {% endfor %}
      {% endfor %}
    </section>

    <section class="haut">
      <p class="center"><a href="" class="load">Voir plus</a></p>
    </section>

  </div>
{% endblock %}




